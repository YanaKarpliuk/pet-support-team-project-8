
import { Log,LogAccent} from "./Logo.styled";

const Logo = () => { 
    return (
     <nav>
     
       <Log to="/">
        pe<LogAccent>t</LogAccent>ly
       </Log>
   
     </nav>
    );
}

export default Logo;